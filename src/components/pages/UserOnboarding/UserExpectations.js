// This is the last page that the user will see once they are signed up. It should take the user to the home page/dashboard next

import React from 'react';

const UserExpectations = () => {
  return (
    <>
      <h1>This is the user expectations component!</h1>
    </>
  );
};

export default UserExpectations;
