import React, { useState } from 'react';
import styled from 'styled-components';

const StyledDiv = styled.div``;
function DateForm(props) {
  const { chosenMonth, setChosenMonth, allMonths } = props;

  return (
    <StyledDiv>
      <input />
    </StyledDiv>
  );
}

export default DateForm;
